import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const AccountScreen = () => <Text style={{ fontSize: 48 }}>Account Screen</Text>;

const styles = StyleSheet.create({});

export default AccountScreen;
